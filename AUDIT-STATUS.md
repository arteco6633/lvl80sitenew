# Статус аудита и рекомендации по скорости

**Дата проверки:** 2026

---

## ✅ Выполнено по AUDIT-DEV.md

| Раздел / приоритет | Пункт | Статус |
|--------------------|--------|--------|
| **Высокий** | meta description, title, `lang="ru"` в index.html | ✅ |
| **Высокий** | Формы: заглушка «Спасибо» + примечание про продакшен | ✅ |
| **Средний** | Разбить App.tsx на секции | ✅ Все секции в `src/sections/`: Services, Projects, Cta, Tariffs, FAQ, Contacts, Footer |
| **Средний** | Разбить App.css по блокам | ✅ Стили в `src/styles/`: layout, hero, header, services, projects, cta, tariffs, faq, contacts, footer, modals |
| **Средний** | framer-motion / неиспользуемые UI | ✅ Использование задокументировано в `src/components/ui/README.md` |
| **Низкий** | Переименовать изображения (MOCKUP-5, MOCKUP-3) | ✅ |
| **Низкий** | Фокус в модалках (return focus) | ✅ |
| **Низкий** | Константы в src/data/ | ✅ `projects.ts`, `faq.ts` |
| **Доп.** | Формы: label + id/htmlFor на лендинге и в ContactModal | ✅ |
| **Доп.** | Удалён неиспользуемый CSS: `.hero-title-neon`, `neon-flicker` | ✅ |

---

## ⚠️ По желанию (не блокеры)

- **og:image** — для расшаривания в соцсетях добавить `<meta property="og:image" content="https://...">` (URL превью-картинки).
- **Ассеты в корне** — по возможности перенести `Bb_wheels.png`, `Rocket.svg` и др. в `public/` или `src/assets/`.
- **Валидация форм** — при подключении бэкенда добавить валидацию и вывод ошибок.
- **Оптимизация картинок** — сжатие PNG (TinyPNG, ImageOptim) и/или WebP/AVIF с `<picture>` для тяжёлых слайдов.

---

## 🚀 Что сделано для скорости («чтобы сайт летал»)

1. **Картинки проектов** — `loading="lazy"` и `decoding="async"` в ProjectsSection (тяжёлые PNG ниже первого экрана).
2. **Картинка в модалке проекта** — `decoding="async"`, чтобы не блокировать отрисовку при открытии.
3. **Футер** — иконки и декоративный SVG с `loading="lazy"`.
4. **Шрифты** — `preconnect` на `fonts.cdnfonts.com` в `index.html` для ускорения загрузки шрифтов.
5. **Мёртвый CSS** — удалены неиспользуемые `.hero-title-neon` и `@keyframes neon-flicker` из `index.css`.

---

## 📋 Рекомендации для дальнейшего ускорения

1. **Критичный CSS** — вынести стили выше сгиба (hero, header, layout) в инлайновый блок в `<head>` или подключать их первыми; остальные — асинхронно или по мере надобности.
2. **LCP-картинка** — для главного изображения в hero (если появится фото/баннер) задать `fetchpriority="high"` и явные `width`/`height` для избежания сдвига контента (CLS).
3. **Rocket.svg** — в CtaSection SVG ~3.7 MB; заменить на облегчённую версию или инлайновый SVG, либо отдавать с сервера сжатый вариант.
4. **Код-сплит** — модалки (ContactModal, ProjectModal) подгружать через `React.lazy()` + `Suspense`, чтобы не тянуть их в начальный бандл.
5. **Изображения проектов** — конвертировать в WebP (с fallback в PNG) и при необходимости ресайз под типичные размеры карточек; указать `width`/`height` в разметке.
6. **Шрифты** — оставить только нужные начертания Involve; при возможности перейти на самохостинг из `public/` и отключать cdnfonts для критичного пути.
7. **Сборка** — проверить `vite build` с `--mode production`; при росте приложения рассмотреть разбиение чанков (manualChunks в Rollup).

После внедрения пунктов 2–5 и оптимизации Rocket.svg загрузка и отзывчивость сайта заметно улучшатся.
